# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

stats.html
all_code.txt

.temp/

# Generated planning and audit documents
docs/
coverage/
.workbuddy/
.workbuddy-ai/
.trae/
.wrangler/
.pp-probe/

# 环境变量文件
# .env.development：本地开发覆盖（deploy-worker.mjs 默认/--dev 读取），不入库。
.env.development
# .env.production：部署凭据 —— CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID / SERVER_TOKEN。
# worker/scripts/deploy-worker.mjs 的注释声称它「已被 git 忽略」，但此前 .gitignore 里
# 只有 .env.development（`git check-ignore .env.production` 无匹配）→ 一次生产部署即入公开仓库。
# 注意 .env 本身已被跟踪（仅两个 VITE_ 公开值，尚无泄漏）；gitignore 对已跟踪文件无效，
# 若将来要往 .env 里放任何密钥，必须先 `git rm --cached .env`。
.env.production
.env.production.*
.env.local
.env.*.local
# worker/.dev.vars：wrangler 本地开发密钥（SERVER_TOKEN 等）。wrangler 自身只认这个文件名，
# 不跟随 .env.* 通配，故必须单独列一行——否则一次 `wrangler dev` 的本地凭据就会被提交。
worker/.dev.vars
worker/.dev.vars.*
.zcodeignore
.workbuddy

# 宿主 rules 通道目录：`.codebuddy/rules/` 下是根级 rules/ 的**逐字节副本**（外加一份
# 00-index.md），由 scripts/build-guidance.mjs 派生（与 AGENTS.md 同源），属构建产物故不入库；
# 克隆后由 `pnpm prepare`（安装时自动跑）或 `pnpm guidance:build` 生成。
# 该目录同时是宿主的扫描目录，按 rules/05 的例外允许放注入探针，所以整目录一并忽略。
.codebuddy/
