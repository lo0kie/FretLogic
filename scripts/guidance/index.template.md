---
alwaysApply: true
---

> ⚙️ 本文件由 `scripts/build-guidance.mjs` 生成，**勿手改**。正文的源头是**项目根 `rules/`** ——本目录下的 `NN-*.md`
> 是它的**逐字节副本**（重生成：`pnpm guidance:build`；`.codebuddy/` 不入库）。

## 本目录是什么

根级 `rules/` 下 {{RULES_COUNT}} 份准则正文（合计 {{RULES_CHARS}} 字符）在这里**整目录**被宿主注入，而项目根 `AGENTS.md`
走的 guidance 链有
**8000 字符硬上限**（正文远超这个量级，会被静默截断）。所以本目录是**不受上限约束**的那条通道：两份内容同源、都由
`rules/` 派生，正文以本目录为准。

改准则一律改根级 `rules/` 下的文件，再跑 `pnpm guidance:build`（`pnpm install` 的 `prepare` 也会跑）。

## 三条绝对红线（摘要，以正文为准）

1. **禁止主动重构**：没有「重构 / 拆分 / 整理架构」这类明确指令，就不动架构、不挪文件、不改分层；需求路径之外发现的问题只在回复里提示。→
   `{{REF:01}}`
2. **禁止全项目级验证命令**（WorkBuddy 环境）：`pnpm verify` / `test` / `typecheck` / `lint` / `format:check` / `build`
   / `bench` 一律不代跑，只提示用户自行执行；仅允许文件级检查。→ `{{REF:08}}`
3. **禁止征询式提问**：本可自行决定的事不许包装成问题退回给用户。**开场说声「你好」再问「你希望我怎么称呼你」「你想给我起个什么名字」，是典型违规**
   —— 称呼与命名跟交付无关，自己定或不提；「你说一句我就做 X」同样等价于提问。→ `{{REF:09}}`

> 摘要只为兜底。它与正文冲突时**以正文为准**，且**不得**被当作「省掉读正文」的理由。 **这三条从会话第一句话起生效**
> —— 开场问候不是例外。
