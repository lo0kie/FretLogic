# AGENTS.md — 项目工作规范

本文件作为工作区规则在每轮会话自动注入，指导编码、审查与验证行为。

## 魔法数字 / 字符串

- 业务逻辑中的延时、阈值、尺寸、随机区间等魔法数字，以及可作为键名/标识符的魔法字符串，一律集中到
  `src/utils/core/constants.ts`（按领域分组，配中文注释），不在各业务文件里散落裸字面量。
- 抽取/重构不得改变运行行为，纯语义等价替换。
- 已有命名的本地常量（如 provider 的 `TIMEOUT_MS`、store 的
  `FLUSH_DELAY/FLUSH_MAX_WAIT`）若语义仅限该模块，可保留本地，不强求上提。

## 审查 → 改动流程

- 「检查 / 审查 / 扫描」类请求：先只读审计，输出按优先级（高/中低）分组的报告，明确标注哪些该改、哪些可保留，**等用户确认后再动手**；用户给出肯定指令（如「做吧」）才执行修改。
- 每次代码改动完成后，用 `pnpm lint` + `pnpm typecheck` + `pnpm build` 校验，全部通过才算完成；留意 Vite HMR /
  vue-tsc 增量缓存可能产生的过期编译产物，必要时清除缓存（如 `*.tsbuildinfo`）。
- 涉及 Git 历史重写、force-push、云同步等破坏性操作：先备份、先和用户确认，不默认执行。

## 代码风格

- 业务逻辑与注释：尽可能加中文注释说明意图；文档类注释保持准确、精简。
- Vue 模板内不得出现 HTML
  / 模板注释；无障碍文本、较长展示字符串抽成 computed（单实例）或方法（v-for 循环内），保持模板可读。
