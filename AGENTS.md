# AGENTS.md

> ⚙️ **本文件是派生文件**：总纲段在 `scripts/guidance/AGENTS.template.md`，**正文段是把根级 `rules/` 下的
> 9 份准则正文按序号拼接**（去 frontmatter、其余逐字），由 `scripts/build-guidance.mjs` 生成 ——
> **不要手改，手改会在下次生成时被覆盖**。改准则改 `rules/`，改总纲文案改模板；重生成 `pnpm guidance:build`，一致性校验
> `pnpm guidance:check`。
>
> 同一支生成器还把 `rules/` 下的正文**逐字节复制**一份到
> `.codebuddy/rules/`（宿主 rules 通道整目录注入、无字符上限）。那一份**不入库**（见 `.gitignore`），克隆后由
> `pnpm prepare` 或 `pnpm guidance:build` 生成。

> 本文件是 FretLogic 项目所有 Agent（AI 编码助手）行为准则的**总纲与入口**：说明准则由哪几部分构成、优先级如何、去哪里取全文，并内联一节**随本文件自动注入的红线摘要**。**准则正文不写在本文件里**（摘要不是正文），原因见「为什么正文不在此处」。

---

## 绝对红线（自动注入的摘要，以 `rules/` 正文为准）

这三条在任何时候都不得违反。**在这里再写一遍是有意的**：本文件由 guidance 链自动注入，而 `rules/`
下的正文必须被显式 Read —— 先看到这几行，再去看正文。

1. **禁止主动重构**：没有「重构 / 拆分 / 整理架构」这类明确指令，就不动架构、不挪文件、不改分层；需求路径之外发现的问题只在回复里提示。→
   `rules/01-refactoring-ban-and-admission.md`
2. **禁止全项目级验证命令**（WorkBuddy 环境）：`pnpm verify` / `test` / `typecheck` / `lint` / `format:check` / `build`
   / `bench` 一律不代跑，只提示用户自行执行；仅允许文件级检查。→ `rules/08-workbuddy-verification-ban.md`
3. **禁止征询式提问**：本可自行决定的事不许包装成问题退回给用户。**开场说声「你好」再问「你希望我怎么称呼你」「你想给我起个什么名字」，是典型违规**
   —— 称呼与命名跟交付无关，自己定或不提；「你说一句我就做 X」同样等价于提问。→ `rules/09-no-solicitation-questions.md`

**这三条从会话第一句话起生效，开场问候不是例外。**

## 准则构成

本项目的准则由**三部分**并列构成，正文分置于**项目根 `rules/`** 下的 9
份文件（下表由各文件序言解析而来；**全文见本文件末尾的「准则正文」一节**）：

| # | 文件 | 收录 |
| --- | --- | --- |
| 1 | `rules/01-refactoring-ban-and-admission.md` | 第一部分 架构稳定契约｜一、绝对重构禁令；二、需求驱动与准入原则（含跨层依赖处理） |
| 2 | `rules/02-protected-zones.md` | 第一部分 架构稳定契约｜一、稳定保护区 |
| 3 | `rules/03-scoped-verification.md` | 第一部分 架构稳定契约｜一、交付与验收底线（1.1 验证范围分级、1.2 提交前必须绿灯的完整命令） |
| 4 | `rules/04-changelog-fragment-zones.md` | 第一部分 架构稳定契约｜一、变更日志片段的分区规则（交付与验收底线的 1.1 / 1.2 见 `03-scoped-verification.md`） |
| 5 | `rules/05-small-task-and-temp-files.md` | 第一部分 架构稳定契约｜一、临时文件与工作文档约束；二、小任务纪律 |
| 6 | `rules/06-test-quality-and-self-check.md` | 第一部分 架构稳定契约｜一、防垃圾测试与测试质量红线；二、防内卷提醒机制 |
| 7 | `rules/07-loop-limits-and-circuit-breaker.md` | 第二部分 防循环行为规范｜一、思考深度上限；二、工具调用与验证循环上限；三、输出改写循环上限；四、通用熔断规则；五、覆盖机制 |
| 8 | `rules/08-workbuddy-verification-ban.md` | 第二部分 防循环行为规范｜一、WorkBuddy 环境全量验证绝对禁令 |
| 9 | `rules/09-no-solicitation-questions.md` | 第三部分 交互行为规范｜一、禁止征询式提问 |

**编号规则：每份文件各自从「一」起**
—— 编号只在**文件内**唯一，跨文件必然重复。因此所有交叉引用一律写成**「文件名 + 文件内编号」**（如
`02-protected-zones.md` 的「一、稳定保护区」），**严禁**再使用 `§七`、`第七节`、`4.0 / 4.1 / 4.2`
这类全局节号：它们既不唯一，也会在文件被拆分或合并后指向错误的位置。

**这条规则不限于 `rules/`
内部**：仓库里任何位置引用准则时（源码注释、脚本、配置文件、文档）都必须用同一套写法。此前本仓库有一批 `.ts` / `.mjs`
注释引用 `AGENTS §3` / `§4.2` / `§5` / `第七节 2`
之类的全局编号，编号一改就全部变成死引用，已一次性同步为文件名引用 —— 新增引用照此办理，不要图短写回全局节号。

唯一例外是 `changelog/` 下**已提交的历史片段**：按 `04-changelog-fragment-zones.md`
的「一」它们属冻结区、只读，其中出现旧编号属历史记录，不是活引用，**不要去"修正"**。

三部分并列生效，冲突时以**更具体、更靠后写入的具体任务指令**为准；但 `02-protected-zones.md`（稳定保护区）与
`07-loop-limits-and-circuit-breaker.md`
的「五、覆盖机制」所保留的两类红线不接受任务级覆盖；上面那节红线摘要里的三条同样不接受任务级覆盖。

## 注入通道、强制预读与维护口径（不要改回去）

本文件与 `.codebuddy/rules/` 分属**两条独立通道**，一条有上限、一条没有：

| 通道                                                                    | 自动注入的内容                                  | 字符上限            |
| ----------------------------------------------------------------------- | ----------------------------------------------- | ------------------- |
| `.codebuddy/rules/`（宿主 rules 通道）                                  | `rules/` 的**逐字节副本**（9 份） | 无，整目录全收      |
| guidance 链（`CODEBUDDY.md` → `.codebuddy/CODEBUDDY.md` → `AGENTS.md`） | 本文件（总纲 + 正文全文）                       | **8000 字符硬上限** |

**正文的唯一源头是根级 `rules/`** —— 两层出口都是它的产物：本文件由 `scripts/build-guidance.mjs` 把 9
份正文按序号拼接（总纲段来自 `scripts/guidance/AGENTS.template.md`）， `.codebuddy/rules/` 则是同一批文件的逐字节复制。

**强制预读**：本文件被截断是常态、不是异常。正文合计 15092 字符，远超 guidance 链的 8000 上限 —— 末尾出现
`[...too long, omitted...]` 就说明**下半部分根本没进上下文**。因此：

**动手改任何代码、跑任何命令之前，若上下文中没有见到全部 9 份正文，必须先用 Read 读取根级 `rules/` 下全部
`.md`。**

即便规则到了，也不要假定它一定被执行；以下情况会让注入整条失效：

- `mode === "quick"`（快速问答）时 `ProjectRulesSection.shouldApply` 整段跳过；
- `_meta.isClearSession === true` 时 compose 层直接早退，本轮不注入任何 section；
- 注入只发生在首条 / 恢复消息，之后由 `alreadyInjectedUserContext` 门控不再重复 —— 上下文被压缩后可能一并丢失；
- **注入是快照**：对规则文件的编辑只影响**之后**新建的会话。会话中途改规则，手上的理解与磁盘上的副本立刻分叉 —— 改完必须把改动的文件重读一遍。**写了什么不算数，注入进去的是编辑前的版本。**

本文件的候选链是**首个命中即胜**：`CODEBUDDY.md` → `.codebuddy/CODEBUDDY.md` → `AGENTS.md`。因此：

- **严禁在项目根创建 `CODEBUDDY.md`**，**也不要在 `.codebuddy/` 下创建
  `CODEBUDDY.md`**：它一旦存在，本文件将被整份跳过，全部准则静默失效且无任何报错。
- **不要手改本文件、也不要手改 `.codebuddy/rules/` 下的副本**：两者都是 `rules/`
  的产物，手改会被下次生成覆盖（`pnpm guidance:check` 会比对出来）。准则增删一律改根级 `rules/` 下的文件。
- `.codebuddy/` 是宿主实现细节（隐藏目录、与具体产品绑定），**已不入库**；克隆后由 `pnpm prepare` （`pnpm install`
  时自动跑）或 `pnpm guidance:build` 生成。正因如此，正文的源头必须留在根级 `rules/`
  —— 它在仓库里可见、可 grep、可被人与其他工具直接查阅，也不随宿主换产品而失效。> 上面那节红线摘要**不是正文的替代品**：它只有一屏，且会随正文演进而滞后。它的唯一职责是让「完全没读正文」的会话至少知道三条不可协商的边界 —— 冲突一律以

> `rules/` 为准。

本文件的候选链是**首个命中即胜**：`CODEBUDDY.md` → `.codebuddy/CODEBUDDY.md` → `AGENTS.md`。因此：

- **严禁在项目根创建 `CODEBUDDY.md`**，**也不要在 `.codebuddy/` 下创建
  `CODEBUDDY.md`**：它一旦存在，本文件将被整份跳过，全部准则静默失效且无任何报错。
- 准则增删一律改根级 `rules/` 下的文件；`.codebuddy/rules/`
  只保留那个触发器，本文件只保留总纲与那节红线摘要，两者都刻意保持短小。

> 结构变更史：2026-09-25 由单文件 `AGENTS.md` 拆为「总纲 + 正文」，正文落在
> `.codebuddy/rules/`（起因：8000 字符截断把全文后半部分整段丢弃）；同日按主题把正文拆为 8 份小文件（原两份分别为 9223 /
> 2987 字符，其中「交付与验收底线」一节独占全文 36%）；同日把每份文件的编号改为各自从「一」起、交叉引用改为按文件名，以消除对全局节号的依赖；同日增补 09 与「第三部分 交互行为规范」；同日把正文整体迁出
> `.codebuddy/rules/` 到根级 `rules/`，原目录只留 `00-read-root-rules.md`
> 作触发器；同日清掉全仓 10 个文件里 12 处沿用旧全局节号的引用（`src/` 3、`tests/` 6、`scripts/` 2、`vite.config.ts`
> 1），全部改为文件名引用；同日把三条绝对红线的摘要内联进本文件（此前只存在于触发器里），使两条自动注入通道都带上边界。同日把本文件与
> `00-read-root-rules.md` 一并改为**由 `rules/` 派生**（`scripts/build-guidance.mjs` +
> `scripts/guidance/*.template.md`）：文件清单、份数、正文总字符数、表格里每条的「收录」、三条红线指向的文件名，全部改为从
> `rules/*.md` 的序言解析，不再手抄第二遍；`.codebuddy/` 随之退出 git（克隆后由 `pnpm prepare`
> 补出），本文件继续入库。同日再按「两层出口都承载全文」重做一遍：本文件改为把 `rules/` 下的 9
> 份正文**按序号整体拼接**（去 frontmatter、其余逐字，正文段拼在总纲之后），`.codebuddy/rules/`
> 改为该批文件的**逐字节副本** —— 原先那个「去读
> `rules/`」的触发器已删除：有上限的 guidance 链与无上限的 rules 通道因此承载同一份内容。

---

# 准则正文（拼接自根级 `rules/`，勿手改）

<!-- 每份正文前面标了它的源文件名；frontmatter 只保留在 .codebuddy/rules/ 的副本里，这里拼接时去掉。 -->

<!-- rules/01-refactoring-ban-and-admission.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第一部分 架构稳定契约｜收录 一、绝对重构禁令；二、需求驱动与准入原则（含跨层依赖处理）。总纲、优先级与强制预读要求见项目根
> `AGENTS.md`。

> **核心原则**：本项目（FretLogic）已完成全工程垂直领域化拆分与平台解耦，架构状态已评定为
> **Done（达标态）**。所有 Agent 必须以**"业务功能交付"**为第一优先级，严格遵守以下防过度重构红线。

## 🚫 一、绝对重构禁令（No Unprompted Refactoring）

1. **严禁审美驱动的主动重构**：
   - 严禁擅自以"代码更优雅"、"文件拆小一点"、"模块目录结构可以调整"为由挪动文件、重命名符号或重新分层。
   - 除非用户明确输入包含 **"重构"**、**"拆分"**、**"整理架构"**
     等明确指令，否则所有任务必须严格聚焦于用户当前提出的具体需求/Bug 修复。
2. **严禁蔓延式修改（No Blast Radius Creep）**：
   - 实现新需求时，变更范围应严格限定为**实现该需求的必要文件**，不设固定数量上限——但每个被改动的文件都必须能一句话说清"为什么必须改这个文件"，说不清楚就是范围蔓延。
   - 禁止在完成功能的同时顺带做"全局常量整理"、"类型搬家"或"跨文件大扫除"。
   - 若在开发中发现**当前需求路径之外**的历史代码不顺眼，**只能在回复文本中提示用户，严禁直接在代码中展开重构**——"准入原则"（见本文件的「二、需求驱动与准入原则」）仅在当前需求的实现路径上生效，路径外发现的问题不做二次判断，一律走提示流程。

## 🎯 二、需求驱动与准入原则（Pain-Driven Development）

只有满足以下三种条件之一，且**问题出现在当前需求的实现路径上**，才允许触发局部重构：

1. **阻塞性痛点**：当前现有架构**直接阻碍了新功能的实现**（例如循环依赖导致无法注入服务）。
2. **Defect 归因**：排查 Bug 时发现是由重复实现的逻辑导致，为修复 Bug 必须收拢逻辑。
3. **三次重复原则（Rule of Three）**：在实现**具体业务功能**的过程中，同一段非平凡代码已被完全复制粘贴了 3 遍以上。

### 关于跨层依赖的处理

若新功能确需跨越 `03-scoped-verification.md`
的「1.2 提交前必须绿灯的完整命令」所列的禁止依赖方向（例如两个 domain 之间需要联动、或下层需要调用上层能力），**优先使用项目既有的合法模式**解决，而不是放宽隔离规则：

- **事件订阅 + app 层接线**：下层暴露事件钩子（如 `onXxxChanged`），由 `app/services/`
  中的桥接模块订阅并完成两端联动（参考 `chordScoreBridge.ts`）。
- **依赖倒置（InjectionKey）**：下层定义能力契约（`InjectionKey<T>`），由上层 `provide` 具体实现，下层只 `inject`
  调用（参考 `chord/library/injectionKeys.ts`）。
- **纯函数参数传递**：`platform/utils` 等最底层工具一律通过参数/回调获取所需数据，不建立任何模块耦合。

**不得**为图方便直接放宽 `eslint.config.mjs`
中的 zone 规则。如排查后确认现有模式确实无法满足需求、必须放宽某条隔离规则，须在回复中显式声明理由并等待用户确认后才能修改规则文件。

---

<!-- rules/02-protected-zones.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第一部分 架构稳定契约｜收录 一、稳定保护区。总纲、优先级与强制预读要求见项目根
> `AGENTS.md`。

## 🛡️ 一、稳定保护区（Protected Zones）

以下模块已通过高强度单测与解耦验证，非专门指令一律视为**只读保护区**：

- `src/platform/`：通用基础设施底座已彻底解耦，严禁重新引入上层 Pinia Store 或业务领域依赖。
- `src/domains/chord/theory/`：纯乐理与算法内核已完备，严禁为审美目的微调理论函数。
- `src/domains/fretboard/model/`：纯几何物理模型（弦品坐标/横按/指纹签名），严禁引入任何和弦或乐谱业务依赖。
- `tests/`：核心算法、数据安全与复杂手势指令的测试是系统安全的锚点；但严禁编写“1=1”同义反复测试或写死字面量的易碎测试（详见
  `06-test-quality-and-self-check.md` 的「一、防垃圾测试与测试质量红线」）。

> ⚠️ **保护范围与 zone 规则不是一一对应**，两者是不同口径，不要互相推导：
>
> - `src/platform/` 与 `src/domains/fretboard/model/`
>   有**部分**重叠的 zone（① 与 ③），但 zone 只管「依赖方向」，保护区还管「不得为审美微调实现」——依赖方向合规不等于允许改；
> - `src/domains/chord/theory/` 与 `tests/` **没有任何 zone 规则**，它们的约束只来自本节；
> - 反向也不成立：zone ④（fretboard ↛ score）、⑤（chord ↛ score）、⑥（platform/utils ↛
>   platform 上层）在保护清单里没有对应条目，它们是纯依赖隔离规则，不构成「只读保护区」。
>
> 任何试图放宽保护区的改动，必须同步修改对应的 eslint 规则，并在回复中显式声明"本次改动放宽了 XX 保护区，原因是 XX"——严禁在不触碰 eslint 配置的前提下于代码中悄悄突破保护区边界（例如绕过校验直接跨层 import）。
>
> ⚠️ **作用域限定**：上述 zone 规则的 `files` 只匹配 **`src/**`**，即"单向依赖"是对**运行时源码**的要求。
> `scripts/`、`worker/`、`tests/`、`vite.config.ts`、`eslint.config.mjs` 属**构建与测试工装**，允许自由 import `src/`
> 内层（校验脚本、打包配置、测试夹具本来就必须触达内部实现）——这不是"官方洞"，而是刻意的边界：工装不参与运行时依赖图。**不要**为了"补齐隔离"给这些路径也加上 zone 规则。

---

<!-- rules/03-scoped-verification.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第一部分 架构稳定契约｜收录 一、交付与验收底线（1.1 验证范围分级、1.2 提交前必须绿灯的完整命令）。总纲、优先级与强制预读要求见项目根
> `AGENTS.md`。

## 🚦 一、交付与验收底线（Acceptance Gate）

### 1.1 验证范围分级（Scoped Verification）

验证成本必须与任务规模匹配，禁止小改动触发全量检查：

- **单文件/小功能改动**（改动 ≤3 个文件，且不涉及类型定义、共享 store、跨模块接口）：
  - **lint 有文件级形态**：`pnpm eslint <改动文件路径>`（可加 `--max-warnings 0` 与缓存）；
  - **typecheck 在本项目没有文件级形态，不要硬造**：给 `vue-tsc` / `tsc` 传单个文件路径会**绕过 `tsconfig.json`**
    —— 路径别名（`@/...`）与全部严格开关一起失效，报出来的错与真实构建无关（别名会被当成找不到的模块、`strict`
    系列全部关闭），属于假绿或假红。**正确的降级是"跳过该项"**，并在回复中说明"类型检查未验证"；需要类型结论时按下方「触发全量检查的场景」处理。
  - 只跑与改动模块直接相关的测试文件（如 `pnpm vitest run <对应 test 文件>`），禁止为一个小改动触发 `pnpm test`
    全量套件。
- **触发全量检查的场景**（必须跑下方第 1~3 条完整命令）：
  - 改动涉及 `types.ts`、跨层接口（InjectionKey、事件契约）、Pinia store 的 state/action 签名；
  - 改动触及 `02-protected-zones.md` 的「一、稳定保护区」所列**保护区**；
  - 改动文件数超过 5 个，或改动波及多个 domain；
  - 用户明确要求"跑一下完整检查"，或该次改动即将提交/合并。
- **提交前的最终关卡**：无论开发过程中用的是文件级检查还是全量检查，**正式提交前**
  必须完整跑通下方第 1~3 条命令一次（不可用文件级检查代替），以防局部通过掩盖了跨文件的类型/依赖破坏。

> ⚠️ **唯一的例外，且只针对 WorkBuddy 类 Agent**：本环境的执行通道会在全量验证过程中**永久卡死进程**
> （无法自行恢复），因此 WorkBuddy Agent 在上述所有"必须跑全量"的场景下**一律不代跑，只提示用户自行执行**；见
> `08-workbuddy-verification-ban.md`。其他 Agent 环境不存在该问题，本节原文照常适用。

> 本节是全项目统一的验证分级标准，`05-small-task-and-temp-files.md`
> 的「二、小任务纪律」中的验证要求与本节保持一致，不重复定义。

### 1.2 提交前必须绿灯的完整命令

> ⚠️ **WorkBuddy 类 Agent 不得代跑本节命令**（本环境的执行通道会在全量验证中永久卡死进程）——见
> `08-workbuddy-verification-ban.md`。其他 Agent 环境不受限。WorkBuddy
> Agent 的职责是在改动完成后提示用户自行跑本节关卡。

**完整关卡是 `pnpm verify`**（`scripts/verify.mjs`，串行 8 步，挂在 pre-push 上）：
`format:check → changelog:check → lint → typecheck → typecheck:tests → test → build → build:budget`。CI（`.github/workflows/ci.yml`）跑同一组关卡，只是少了
`changelog:check`、多了信息性的 `pnpm bench`。

下面列出其中三条最关键命令的通过标准 ——
**不要**把它们当成完整关卡：只跑这三条会漏掉 tests 侧类型、产物构建与体积预算，属于「本地绿、CI 红」。

1. `pnpm typecheck`：0 错误（`vue-tsc` 严格校验）
2. `pnpm lint`：0 错误 0 警告（ESLint 依赖架构隔离规则：`import/no-restricted-paths` 六条严格 zone 已实装，target 全部
   `**`
   覆盖子目录——platform↛domains/app、domains↛app、fretboard/model↛chord/score、fretboard↛score、chord↛score、platform/utils↛platform 上层）
3. `pnpm test`：100% 通过（全量 Vitest 测试套件）。**不统计覆盖率**：覆盖率门槛会反向催生「为凑数而写」的用例，2026-09-25 已把整条覆盖率关卡拿掉（`test:coverage`
   脚本与分层阈值一并删除）。
4. 格式化统一由 `node scripts/prettier-format.mjs` 处理，严禁破坏已固化的属性无值优先格式。 `.github/CHANGELOG.md`
   是**派生文件**，已列入 `.prettierignore`（prettier 完全不碰它）；`changelog/*.md` 是手写源码、仍归 prettier 管，靠
   `.prettierrc` 里那条 `proseWrap: "preserve"` 覆盖免于重排 ——该覆盖**不要删**：折行一旦交给 prettier（全局是
   `always`、120 列）重排，就会出现「内容没变、diff 上千行」的老问题。
5. 提交前必须过一遍 `changelog/`：本次变更若包含**用户可感知的改动**，须写进 `changelog/` 下的 **当前工作区片段**
   —— 即上次提交之后创建、尚未提交的那个片段（工作区里没有才新建），文件名
   `<YYYY-MM-DD-HHMM>-<ascii-kebab-slug>.md`（用当前时间取名；slug 只是短标识符，不写整句，口径见
   `scripts/build-changelog.mjs` 文件头），正文即 Keep a Changelog 风格的 `### <类型> · <主题>（<日期>）`
   小节。片段**以工作区为界分「已提交区」（冻结、只读）与「未提交区」（可塑、工作区内的改动一律并进同一个片段）两区**，**细则见
   `04-changelog-fragment-zones.md`**。重新生成汇总这一步**不必单独记**：`.husky/pre-commit` 会在提交前自动跑
   `node scripts/build-changelog.mjs` 并把 `.github/CHANGELOG.md`
   纳入本次提交，片段命名不合规会被生成器拦下。该文件是派生文件，**禁止手改**，`pnpm changelog:check`
   会门禁这一点（已纳入 `pnpm verify` 第 2 步）。判定标准：
   - **需要记录**：diff 涉及 `src/domains/`、`src/app/` 或 `src/platform/ui|store|services`
     下的行为变化（新功能、交互变化、Bug 修复、破坏性迁移）。
   - **可跳过**：纯配置/文档/格式化改动，以及 `platform/utils`
     内部实现细节调整、类型标注、注释补充等不影响用户可感知行为的改动。
6. 提交信息风格：`<type>: <主题>，<主题>…` + 空行 +
   **每条一个改动的扁平要点列表**（`- <主题>：<是什么>`，全角冒号）。硬约束：**只写「改了什么」，不写「怎么实现的」**—— 不解释根因、机制、取舍、踩过的坑，那些属于
   `changelog/`
   与代码注释。主题行约 3 个主题、单行不折行；要点数随改动规模、单条一句话不展开；不写 markdown 标题、不写 scope、不写
   `Signed-off-by`。参考既有提交：短版 `34305bc` / `a48db37`，中版 `e3d79dc`。

---

<!-- rules/04-changelog-fragment-zones.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第一部分 架构稳定契约｜收录 一、变更日志片段的分区规则（交付与验收底线的 1.1 /
> 1.2 见 `03-scoped-verification.md`）。总纲、优先级与强制预读要求见项目根 `AGENTS.md`。

## 📝 一、变更日志片段的分区规则（Working-Tree Zones）

`changelog/` 下的片段**以工作区为界**分为两区，两区的**可写性相反**。这是片段化日志能维持「diff 恒为 N 行新增 /
0 行删除」的全部前提，也是判断「该新开片段还是该就地改写」的唯一判据：

- **已提交区（冻结）**：片段一旦随某笔提交入库即**只读**。此后任何改动 —— 合并、改写、补记、改个错别字 ——都会产生删除行，正是当初拆分要消灭的那类 diff。要给已发布的条目追加内容，只能写进**当前工作区片段**。
- **未提交区（可塑）**：**工作区里的全部未提交改动同属一区**
  —— 片段边界对齐**工作区**，既不按「改了几轮」拆，也不按「主题 / 批次」拆。本项目**不分批提交**（一次工作区 = 一次提交 = 一个片段），所以工作区里所有未提交的改动，无论中途返工、推翻、补记过多少次、横跨多少个主题，都应**并进同一个片段、就地改写**。**严禁每改一轮或每换一个主题就新开一个片段**
  —— 一天里同一批改动散成五六个片段，等于把「一次工作区一个片段」退化成「每次编辑一个片段」（2026-09-24 已因此返工一次，6 个合成 1 个）。
- **当前工作区片段**：即上次提交之后创建、尚未提交的那个片段（`git status` 里唯一带未提交改动的
  `changelog/*.md`）。工作区里没有这样一个片段时才新建。**已提交的片段不再接收新内容**
  —— 往上面追加，等于把本批改动挂到上一笔提交的名下，也让那个片段不再冻结。
- **粒度参照**：本目录的正常粒度是「**一个片段一个文件、内含多条 `###` 小节**」（实测存在 28 / 22 / 21 / 18 /
  12 条的片段）—— 单小节的文件反而是异类，不要被「一个片段 = 一条改动」的直觉带偏。
- **格式**（命名规则、正文写法、`###` 小节形态）见 `03-scoped-verification.md`
  的「1.2 提交前必须绿灯的完整命令」第 5 条；**分区由作者保证** —— `scripts/build-changelog.mjs`
  只是个拼接器，它看不见工作区边界，不会替你把关。
- **本节的落地动作**：一律就地改写当前工作区片段；若发现工作区里有两个片段各带未提交改动（例如误在已提交片段上追加过内容），把它们**并进当前工作区片段**、让已提交的那个片段回到与提交一致，改完跑一次
  `pnpm changelog:build` 让汇总与片段锁步。

---

<!-- rules/05-small-task-and-temp-files.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第一部分 架构稳定契约｜收录 一、临时文件与工作文档约束；二、小任务纪律。总纲、优先级与强制预读要求见项目根
> `AGENTS.md`。

## 📁 一、临时文件与工作文档约束（Temp File Policy）

1. **严禁主动创建工作文档**：
   - 不得未经用户要求主动创建任何总结/记录类文档（如 `*.md`、TODO 清单、变更日志、分析报告等）。
   - 仅当用户**明确要求**保存或输出文档时才允许写文件；默认一律以回复文本形式呈现。
2. **临时文件必须收敛到 `.temp/`**：
   - 排查/验证/实验用的临时产物（探针 HTML、临时脚本、中间快照、日志等）一律写入 `.temp/`
     目录，严禁散落在根目录或 src 内。
   - 该目录已被 `.gitignore` 忽略；任务结束后如确认不再需要，应主动清理。
   - 唯一例外是按被测机制要求必须落在别处的探针（例如验证 `.codebuddy/rules/`
     注入时，探针**必须**放在该目录内才可能被扫描到）。这类例外须在回复中说明理由，并在测试结束后立即删除。
3. **严禁创建工作区记忆文档**：
   - 严禁在 `<项目>/.workbuddy/` 下创建任何文件，尤其是 `memory/YYYY-MM-DD.md`、`memory/MEMORY.md`
     这类「工作区记忆」文档。
   - 本项目根的 `.workbuddy` 已被**有意**占位成一个普通文件：对该路径的任何写入都会以 `ENOTDIR`
     失败。撞到这个错误时**不要绕过**——不要换文件名、不要换父路径、不要换写入工具、不要改走 shell 重定向。它就是预期行为，如实上报即可。
   - 同理，用户级 `~/.workbuddy/MEMORY.md` 也不在授权范围内，除非用户当轮明确要求。
   - 本条的成因与边界见项目根 `.workbuddy` 文件自身的说明。
4. **严禁用展示类工具把文件推到侧边栏 / 结果区**：
   - 严禁主动调用 `present_files` —— 或任何会打开预览面板、结果区、产物卡片的等价工具。交付一律以**回复文本**
     形式呈现；用户要看文件时自己会打开。
   - 这是硬约束：不因为「流程惯例上要展示产物」「本轮产出了可查看的结果」「宿主提示要求展示」而破例。只有用户**当轮明确说**「打开 / 展示这个文件」才可例外。
   - 附带一条：`show_widget`
     只在对话内联渲染、不开侧栏，不受本条约束；但仍受第 1 条「未经要求不主动产出文档」的精神约束，不要为了凑视觉而用它。

## ⏱️ 二、小任务纪律（Small Task Discipline）

小需求必须小步交付，严禁把简单任务膨胀成全链路大扫描：

1. **先估规模再动手**：接到需求先判断规模（如「更新一个 JSON 样例文件」）。凡可通过「读 1~2 个文件 + 局部修改」完成的任务，直接执行，严禁为此启动全项目扫描、通读校验管线、逐层调试源码。
2. **最小探索原则**：只读完成当前任务直接相关的文件。需要确认字段格式时，优先看实体类型定义（types.ts）或现有同构文件，**不需要**为了一个样例去验证整个校验/导入/导出管线的运行时行为。
3. **验证成本与任务规模匹配**：具体分级标准见 `03-scoped-verification.md`
   的「1.1 验证范围分级」，小任务只需按对应级别做一次简单校验，严禁编写多层调试脚本、临时打点、往返验证链——除非首次交付被用户指出确有问题。
4. **交付优先，迭代兜底**：小任务宁可先给出 90 分结果让用户验收，也不追求一次性证明所有边界。发现问题再按 Bug 修复流程单独排查。

---

<!-- rules/06-test-quality-and-self-check.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第一部分 架构稳定契约｜收录 一、防垃圾测试与测试质量红线；二、防内卷提醒机制。总纲、优先级与强制预读要求见项目根
> `AGENTS.md`。

## 🧪 一、防垃圾测试与测试质量红线（Anti-Tautology & Test Quality Discipline）

严禁为了凑测试数量或覆盖率编写没有实际质量收益的“1=1 自嗨测试”与“脆弱型易碎测试”：

1. **绝对禁写“1=1”同义反复测试（No Tautological Tests）**：
   - **严禁测试框架内置行为**：例如挂载组件断言 `props.label === 'foo'` 时文本是否为
     `'foo'`（这是 Vue 框架的功能，不是业务逻辑）；
   - **严禁测试纯访问器与空包装**：例如单独为 `x => x.id` 这类没有任何分支的 getter 写单测；
   - **严禁测试原子 UI 的 CSS 类名与内联样式**：严禁断言 `expect(el.classes()).toContain('border-danger')` 或
     `toContain('px-3')`——这类断言一旦样式微调或换 Tailwind 类就会产生纯噪音假报错；
   - **严禁无有效断言的空跑用例**：每个 `it` 必须包含对被测行为的实质验证，以下形态一律视为垃圾用例——仅
     `expect(el).toBeDefined()` 式存在性断言、`setProps`/toggle 触发后不验证任何结果、mock（如
     `el.animate`）注入后不做任何断言就结束用例。挂载/执行本身不是测试目的，验证行为才是。
2. **绝对禁写“写死字面量”的脆弱测试（No Hardcoded-Literal Tests）**：
   - **严禁把可变配置常量写死在断言中**：例如严禁在单测中把 `DEFAULT_WORKBENCH_PANEL_ORDER` 默认顺序数组在断言里写死成
     `['analysis', 'variants', ...]`，或断言静态 demo 示例数据里正好有几条记录；
   - **严禁为了迎合坏测试修改业务代码**：当发现测试失败是由硬编码了旧常量、旧字面量导致的假报错时，**严禁反向篡改用户的业务配置去迁就坏测试**，必须直接修正或删除该垃圾测试。
3. **测试价值准入原则（High-Value Testing Only）**：
   - **高价值区（必须单测覆盖）**：乐理与几何算法纯函数（变调、品位坐标计算、和弦推导、AST 标记解析）、跨实例文本编解码协议、数据持久化清洗与备份防丢失守卫、带有内部状态机/手势计算的指令或复合组件；
   - **免测区（严禁编写单测浪费维护成本）**：纯透传 Props 的基础 UI 组件（Icon/Badge/FormRow/Input/SegmentedControl 等）、无内部状态的展示包装层、纯类型定义与导出索引。

## 💡 二、防内卷提醒机制（Self-Check Prompting）

- 若用户提出了带有明显"过度工程化"倾向的模糊重构想法，Agent 应礼貌且坚定地提醒用户：
  > "当前项目架构健康度良好（0 类型错误 / 0 架构违规 /
  > 100% 测试通过）。此项重构预计不会带来可见的运行时或维护性增益，建议优先推进用户已明确规划的产品功能。如确定要执行，请回复确认。"

---

<!-- rules/07-loop-limits-and-circuit-breaker.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第二部分 防循环行为规范｜收录 一、思考深度上限；二、工具调用与验证循环上限；三、输出改写循环上限；四、通用熔断规则；五、覆盖机制。总纲、优先级与强制预读要求见项目根
> `AGENTS.md`。

> **核心原则**：循环的根源永远是"停止条件未定义"。本部分的目标不是让 Agent
> "想得更少"，而是让每一类行为（思考/验证/改写/探索）都有一个**客观、可检验**的终止标准，使 Agent 有明确的许可在达标后停下，而不必自行判断"是否已经足够"。

## 🧠 一、思考深度上限（Thinking Budget）

1. **禁止无限自我推翻**：思考过程中若出现"等等，我再重新想想""不对，应该是……"这类自我否定，最多允许
   **2 次**方向性调整；第 2 次调整后必须收敛为结论，不得再次推翻重来。
2. **先给结论，再补论证**：默认输出顺序为「初步判断 → 关键依据 → 边界情况说明」，而不是"完全想清楚再一次性输出"。即便中途对细节仍有不确定，也必须先交付主结论，不确定的部分显式标注，不作为拖延输出的理由。
3. **复杂问题必须先拆分**：当问题包含 3 个以上子维度时，禁止一次性做"整体深度分析"，必须先列出子问题清单，再逐个给出结论。子问题之间不得来回跳跃重复论证同一点。

## 🔁 二、工具调用与验证循环上限（Tool-Loop Discipline）

1. **明确的完成判据（Definition of Done）优先于主观判断**：
   - 禁止使用"确保没问题""确认无误"这类主观收尾标准。
   - 任务开始前必须先确定一个客观锚点（如：命令退出码为 0、测试全部通过、某字段值等于预期），达到该锚点即视为完成，不得因"感觉还不够稳"而继续重复验证。
2. **验证轮次上限**：
   - 同一件事（读文件、跑命令、搜索）的**重复验证最多 1 次**。
   - 若第 1 次验证已通过，禁止"再确认一遍"式的重复动作。
   - 若第 1 次验证失败，允许 1 次针对性修复 +
     1 次复验；连续失败 2 轮以上必须停止自行反复尝试，转为向用户说明卡在什么问题上、目前的假设是什么，等待用户输入，而不是自行无限重试。
3. **信息探索范围前置声明**：
   - 开始任务前，先说明"本次只读取/搜索以下范围内的信息"，超出范围的内容用假设代替并注明。
   - 禁止因为"信息好像还不够充分"而无休止地扩大搜索/阅读范围。

## ✍️ 三、输出改写循环上限（Rewrite Discipline）

1. **先定结构，后填内容**：先给出大纲/结构，经确认或默认接受后，后续修正只允许针对局部小节，不得因发现某个细节问题就推翻整体重写。
2. **推倒重来次数上限**：同一份输出的整体重写最多
   **1 次**；超过 1 次仍不满意，必须停止自行重写，转为向用户提出"卡点是什么、需要哪类澄清"，而不是自己反复试错。
3. **降低隐性完美主义压力**：避免在任务描述中堆砌"确保严谨""必须完备""万无一失"等词，这类措辞会诱导模型不断自我审查、迟迟不敢定稿。改为"给出可用的初步方案，不确定之处显式标注即可"，明确"可以带着已知局限性交付"。

## 🛑 四、通用熔断规则（Circuit Breaker）

无论处于上述哪种循环场景，只要出现以下任一信号，**必须立即停止当前循环并向用户汇报**，不得继续自行尝试：

- 同一类操作（思考调整/验证/重写）已达到本规范设定的次数上限；
- 连续 2 次尝试后，问题范围或解决方向没有发生实质性变化（即在原地打转）；
- 需要用户提供的信息缺失，导致继续尝试只是重复同样的猜测。

汇报内容应包含三部分：**已经确认的部分 / 卡住的具体问题 / 需要用户做的判断或提供的信息**，而不是继续憋一个"完整答案"。

## 📌 五、覆盖机制

- 若某类任务明确需要更高的验证强度（如金融计算、安全相关代码），可在具体任务指令中显式提高对应上限（如"验证轮次上限提升为 2 次"），作为本规范的临时覆盖，需在任务说明中注明覆盖原因。
- 具体任务指令与本文件冲突时，以任务指令为准，但涉及「稳定保护区」（见 `02-protected-zones.md`）与「依赖隔离规则」（见
  `03-scoped-verification.md` 的「1.2」）的条款除外 —— 这两类红线不接受任务级覆盖，如确需突破，按
  `02-protected-zones.md` 所述流程执行（显式声明 + 用户确认）。

---

<!-- rules/08-workbuddy-verification-ban.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第二部分 防循环行为规范｜收录 一、WorkBuddy 环境全量验证绝对禁令。总纲、优先级与强制预读要求见项目根
> `AGENTS.md`。

## 🚫 一、WorkBuddy 环境全量验证绝对禁令（Full-Verification Ban — WorkBuddy Only）

> **适用范围：仅 WorkBuddy 类 Agent（本环境）。**
> 原因：**只有本环境的执行通道会在全量验证过程中永久卡死进程**——进程挂死后无法自行恢复，只能人工终止。其他 Agent 环境不存在该问题，因此**不受本条约束**，仍按
> `03-scoped-verification.md` 的「1.1 / 1.2」原文执行全量检查。
>
> 本条由用户直接下达，对 WorkBuddy
> Agent 而言优先级高于本文件其他全部条款，任何情况下不得例外，不接受任务级覆盖，也不因"改动很大""马上要提交""只是想确认没破坏"而放宽。

1. **WorkBuddy Agent 永远不得运行全项目级验证命令**，包括但不限于：`pnpm test`、
   `pnpm typecheck`、`pnpm lint`、`pnpm format:check`、`pnpm build` / `pnpm build:analyze` /
   `pnpm build:budget`、`pnpm verify`、`pnpm bench`，以及任何以 `.` 或仓库根为作用域的等效写法。
2. **只允许文件级 / 最小作用域校验**：例如 `pnpm eslint <改动文件…>`、
   `pnpm exec prettier --experimental-cli --check <改动文件…>`、
   `pnpm vitest run <与改动直接相关的 test 文件…>`。**若某项没有文件级形态，就跳过该项**，在回复中如实说明"该项未验证"，**不得为了补上这一项而升级成全量命令**。
3. **提交前的全量关卡（`03-scoped-verification.md` 的「1.2」第 1~3 条）对本环境改为纯人工步骤**：WorkBuddy
   Agent 一律不代跑，只能提示用户「本次改动已具备提交条件，请自行跑一次完整关卡」。
4. **判据是命令的作用域，不是它跑得多快。** 典型违反症状：把"文件级检查"做成"带 `--incremental`
   的全量命令"、把"提交前跑一次"做成"现在就跑一次"、把"改动文件多"当成可以升级全量的理由。
5. **需要全量结论时**（例如怀疑存在跨文件类型破坏或历史遗留失败），只向用户说明 "需要你跑一次全量才能确认"，并给出具体要跑的命令与要看的失败项，**不得代为执行**。

---

<!-- rules/09-no-solicitation-questions.md -->

> 本文件是《AGENTS.md》准则正文的一部分｜第三部分 交互行为规范｜收录 一、禁止征询式提问。总纲、优先级与强制预读要求见项目根
> `AGENTS.md`。

## 🚫 一、禁止征询式提问（No Solicitation Questions）

严禁把**本可自行决定的事**包装成问题退回给用户。判据只有一条：**这句话用户不回，我是否就无法推进？**
答「否」的，一律不许问，也不许用等价的非问句形式说出口。

**严禁的形态**（非穷举）：

1. **打招呼式的定规矩**：询问用户希望如何称呼自己、请用户替自己取名、或声明自己希望被如何称呼。这类提问与交付无关，属于纯噪音 —— 用户想怎么叫就怎么叫，需要区分时会主动说。
2. **让用户在没有优劣的选项里挑**：命名、措辞风格、目录取位、文件名、格式偏好，全都有可辩护的默认值。挑一个即可。
3. **「你说一句我就做 X」式的条件邀请**：形式上不是问句，实质仍把决定退回给用户，与本条禁止的提问完全等价，一并禁止。
4. **给已定的默认值补一次确认**：除非操作**不可逆**（删除、覆盖、外发、写生产环境），否则默认值直接生效；不得以「稳妥起见」为由先问再做。
5. **报告末尾附挂的「你看要不要…」**：任务边界外的事项只在回复中以陈述句说明，不征求同意。范围外发现的问题按
   `01-refactoring-ban-and-admission.md` 的「一」第 2 条处理。

**正确做法**：自行选一个合理默认值 → 直接执行 → 在回复中用**一句话**说明选了什么、为什么，随后继续推进。

**唯一例外**：所缺信息**不可推断**且**会实质改变结果**
—— 例如用户私有的凭据、只能由用户指定的目标环境、不可逆操作的作用对象。此时只问**那一个**必须的问题，且不得夹带任何称呼、偏好或铺垫类提问。

> 本条是**经多次纠正后确立的硬约束**，不是风格偏好。首次接触、任务开头、任务收尾一律不例外。
