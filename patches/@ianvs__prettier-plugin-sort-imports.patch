diff --git a/lib/src/utils/explode-type-and-value-specifiers.js b/lib/src/utils/explode-type-and-value-specifiers.js
index 07f78ddc51eed264f7fcbb9c7fdd8c1adb20bf54..38f27445d7a8e8f37c27faae21ce0e8a333b8da4 100644
--- a/lib/src/utils/explode-type-and-value-specifiers.js
+++ b/lib/src/utils/explode-type-and-value-specifiers.js
@@ -21,16 +21,21 @@ const explodeTypeAndValueSpecifiers = (nodes) => {
             explodedNodes.push(node);
             continue;
         }
-        // Nothing to do if there's only one specifier
-        if (node.specifiers.length <= 1) {
-            explodedNodes.push(node);
-            continue;
-        }
         // @ts-expect-error TS is not refining correctly, but we're checking the type
         const typeImports = node.specifiers.filter((i) => i.type === 'ImportSpecifier' && i.importKind === 'type');
+        const valueImports = node.specifiers.filter((i) => !(i.type === 'ImportSpecifier' && i.importKind === 'type'));
+        // FretLogic 扩展：全部为内联 type（import { type A } / import { type A, type B }）时，
+        // 原逻辑因 specifiers.length <= 1 或「非混合」而跳过，这里统一转为独立的 type-only import
+        //（import type { … }），以满足「type 必须单独导入」的契约。
+        if (typeImports.length > 0 && valueImports.length === 0) {
+            typeImports.forEach((specifier) => (specifier.importKind = 'value'));
+            const newTypeNode = (0, types_1.importDeclaration)(typeImports, node.source);
+            newTypeNode.importKind = 'type';
+            explodedNodes.push(newTypeNode);
+            continue;
+        }
         // If we have a mix of type and value imports, we need to 'splode them into two import declarations
         if (typeImports.length && typeImports.length < node.specifiers.length) {
-            const valueImports = node.specifiers.filter((i) => !(i.type === 'ImportSpecifier' && i.importKind === 'type'));
             const newValueNode = (0, types_1.importDeclaration)(valueImports, node.source);
             explodedNodes.push(newValueNode);
             // Change the importKind of the specifiers, to avoid `import type {type Foo} from 'foo'`
diff --git a/lib/src/utils/get-sorted-nodes-group.js b/lib/src/utils/get-sorted-nodes-group.js
index 455ac0c07a330ddf8a613a169dc58500d8ec5024..b831c4efe5c156c62e26115bb79aee0a1b266282 100644
--- a/lib/src/utils/get-sorted-nodes-group.js
+++ b/lib/src/utils/get-sorted-nodes-group.js
@@ -7,6 +7,26 @@ const getSortedNodesGroup = (imports, options) => {
     const sortFn = importOrderCaseSensitive
         ? natural_sort_1.naturalSortCaseSensitive
         : natural_sort_1.naturalSort;
-    return imports.sort((a, b) => sortFn(a.source.value, b.source.value));
+    // FretLogic 自定义排序优先级：默认导入居首 → 命名/命名空间居中 → type-only 沉底。
+    // 优先级相同的 import 仍按模块路径自然排序（沿用原生 comparator）。
+    const getPriority = (node) => {
+        const specs = node.specifiers || [];
+        // 含默认导入（import Foo from … / import Foo, { … } from …）置于组首
+        const hasDefault = specs.some((s) => s && s.type === 'ImportDefaultSpecifier');
+        if (hasDefault)
+            return 0;
+        // type-only 导入（import type { … } from …）沉至组末
+        if (node.importKind === 'type')
+            return 2;
+        // 命名 / 命名空间导入居中
+        return 1;
+    };
+    return imports.sort((a, b) => {
+        const pa = getPriority(a);
+        const pb = getPriority(b);
+        if (pa !== pb)
+            return pa - pb;
+        return sortFn(a.source.value, b.source.value);
+    });
 };
 exports.getSortedNodesGroup = getSortedNodesGroup;
diff --git a/lib/src/constants.js b/lib/src/constants.js
index 78e210a..32e4a35 100644
--- a/lib/src/constants.js
+++ b/lib/src/constants.js
@@ -31,6 +31,9 @@ exports.BUILTIN_MODULES_SPECIAL_WORD = '<BUILTIN_MODULES>';
  */
 exports.THIRD_PARTY_MODULES_SPECIAL_WORD = '<THIRD_PARTY_MODULES>';
 exports.TYPES_SPECIAL_WORD = '<TYPES>';
+// FretLogic 扩展：<SIDE_EFFECT> 分组用于收纳所有无具名导入（side-effect import，
+// 如 import '@/assets/main.scss'），使其可被排序并沉到 importOrder 中该组所在的位置。
+exports.SIDE_EFFECT_SPECIAL_WORD = '<SIDE_EFFECT>';
 const PRETTIER_PLUGIN_SORT_IMPORTS_NEW_LINE = 'PRETTIER_PLUGIN_SORT_IMPORTS_NEW_LINE';
 /** Use this to force a newline at top-level scope (good for newlines generated between import blocks) */
 exports.newLineNode = (0, types_1.expressionStatement)((0, types_1.stringLiteral)(PRETTIER_PLUGIN_SORT_IMPORTS_NEW_LINE));
diff --git a/lib/src/utils/get-chunk-type-of-node.js b/lib/src/utils/get-chunk-type-of-node.js
index 3b60d0a..2111593 100644
--- a/lib/src/utils/get-chunk-type-of-node.js
+++ b/lib/src/utils/get-chunk-type-of-node.js
@@ -32,12 +32,15 @@ const cachedRegExp = (regExp) => {
  * @param importOrderSafeSideEffects An array of regex patterns to consider "safe" side-effect imports.
  * @returns The type of the chunk into which the node should be put.
  */
-const getChunkTypeOfNode = (node, importOrderSafeSideEffects) => {
+const getChunkTypeOfNode = (node, importOrderSafeSideEffects, hasSideEffectGroup = false) => {
     let isSafe = false;
     const hasNoImportedSymbols = node.specifiers.length === 0;
     const isIgnored = (0, has_ignore_next_node_1.hasIgnoreNextNode)(node.leadingComments);
     if (hasNoImportedSymbols) {
-        isSafe = importOrderSafeSideEffects.some((regex) => node.source.value.match(cachedRegExp(regex)) !== null);
+        // FretLogic 扩展：配置了 <SIDE_EFFECT> 分组时，无具名导入视为可排序。
+        isSafe =
+            hasSideEffectGroup ||
+                importOrderSafeSideEffects.some((regex) => node.source.value.match(cachedRegExp(regex)) !== null);
     }
     return isIgnored || (hasNoImportedSymbols && !isSafe)
         ? constants_1.chunkTypeUnsortable
diff --git a/lib/src/utils/get-import-nodes-matched-group.js b/lib/src/utils/get-import-nodes-matched-group.js
index 4d4a7c5..9ad7902 100644
--- a/lib/src/utils/get-import-nodes-matched-group.js
+++ b/lib/src/utils/get-import-nodes-matched-group.js
@@ -21,6 +21,10 @@ const cachedRegExp = (regExp) => {
  * @param importOrder a list of [regexp or special-word] groups (no separators)
  */
 const getImportNodesMatchedGroup = (node, importOrder) => {
+    // FretLogic 扩展：无具名导入（side-effect）统一归入 <SIDE_EFFECT> 分组（若已配置）。
+    if (node.specifiers.length === 0 && importOrder.includes(constants_1.SIDE_EFFECT_SPECIAL_WORD)) {
+        return constants_1.SIDE_EFFECT_SPECIAL_WORD;
+    }
     const includesTypesSpecialWord = importOrder.some((group) => group.includes(constants_1.TYPES_SPECIAL_WORD));
     const groupWithRegExp = importOrder
         .map((group) => ({
diff --git a/lib/src/utils/get-sorted-nodes.js b/lib/src/utils/get-sorted-nodes.js
index 9933c73..8cf8835 100644
--- a/lib/src/utils/get-sorted-nodes.js
+++ b/lib/src/utils/get-sorted-nodes.js
@@ -27,7 +27,8 @@ const getSortedNodes = (nodes, options) => {
     // Split nodes at each boundary between a side-effect node and a
     // non-side-effect node, keeping both types of nodes together.
     const splitBySideEffectNodes = nodes.reduce((chunks, node) => {
-        const type = (0, get_chunk_type_of_node_1.getChunkTypeOfNode)(node, importOrderSafeSideEffects);
+        // FretLogic 扩展：启用 <SIDE_EFFECT> 分组时，无具名导入不再作为排序边界。
+        const type = (0, get_chunk_type_of_node_1.getChunkTypeOfNode)(node, importOrderSafeSideEffects, importOrder.includes(constants_1.SIDE_EFFECT_SPECIAL_WORD));
         const last = chunks[chunks.length - 1];
         if (last === undefined || last.type !== type) {
             chunks.push({ type, nodes: [node] });
diff --git a/lib/src/utils/get-sorted-nodes-by-import-order.js b/lib/src/utils/get-sorted-nodes-by-import-order.js
index 686cef3027457ab92db9dd3604b59368f8496ccc..467765904f801525afc28a9bd8072d430d1b19f5 100644
--- a/lib/src/utils/get-sorted-nodes-by-import-order.js
+++ b/lib/src/utils/get-sorted-nodes-by-import-order.js
@@ -60,9 +60,15 @@ const getSortedNodesByImportOrder = (originalNodes, { importOrder, importOrderCaseSensitive, }) => {
         const groupNodes = importOrderGroups[group];
         if (groupNodes.length === 0)
             continue;
+        // FretLogic 扩展：<SIDE_EFFECT> 分组内不做排序，保留作者手写顺序。
+        if (group === constants_1.SIDE_EFFECT_SPECIAL_WORD) {
+            groupNodes.forEach((node) => (0, get_sorted_import_specifiers_1.getSortedImportSpecifiers)(node, { importOrderCaseSensitive }));
+            finalNodes.push(...groupNodes);
+            continue;
+        }
         const sortedInsideGroup = (0, get_sorted_nodes_group_1.getSortedNodesGroup)(groupNodes, {
             importOrderCaseSensitive,
         });
         // Sort the import specifiers
         sortedInsideGroup.forEach((node) => (0, get_sorted_import_specifiers_1.getSortedImportSpecifiers)(node, { importOrderCaseSensitive }));
         finalNodes.push(...sortedInsideGroup);
